# Observability configuration

Milestone 1 will place the simulator's scrape and alerting configuration here. The initial alert path will use a small deterministic threshold evaluator before Prometheus-compatible exposition is added.
